export function makeHeader() {
    const header = document.querySelector("header");
    const logo = document.createElement("h1");
    logo.textContent = 'udu';
    header.appendChild(logo);
}

export function buildGoalsView() {
    
}